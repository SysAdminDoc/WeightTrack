<?xml version="1.0" encoding="utf-8"?>
<!--
  WeightTrack never uploads a person's body data anywhere. Cloud backup and device transfer
  are both refused so the promise holds without an asterisk; moving to a new phone is done
  with the app's own export and import, which the person controls.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
