<?xml version="1.0" encoding="utf-8"?>
<!--
  Nothing about a person leaves this phone unless they arranged it: an export or backup they
  took, or a folder or server they pointed sync at. Android's own cloud backup and device
  transfer are neither of those, so both are refused here rather than quietly copying a body
  somewhere nobody chose. Moving to a new phone is done with the app's own export and import.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
