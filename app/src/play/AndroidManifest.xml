<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <!--
            Google Play services starts this for each delivery from the watch. The intent filter
            is scoped to this app's own paths so nothing else on the device can reach it.
        -->
        <service
            android:name="com.weighttrack.wear.PhoneWearListenerService"
            android:exported="true"
            android:permission="com.google.android.gms.permission.BIND_WEARABLE_LISTENER">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
                <data android:scheme="wear" android:host="*" android:pathPrefix="/weighttrack" />
            </intent-filter>
        </service>
    </application>
</manifest>
